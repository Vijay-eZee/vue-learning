//@ts-check

/**
 * @typedef {Object} Toast
 * @property {String} name12 some name
 * @property {String} gender
 * @property {Number} id
 * @property {"warn" | "info"} type
 */

/** 
 *  @param {Toast} toast some toast
*/ 
export function showToast(toast) {
    console.log(toast.id)
}
/**
 * it defines student grades
 * @type {Array<string>}
 */
const grads=['A','B','C']

/**
 * its a todo object
 * @type {{id: number|string,name: string}}
 */
const emp = {
    id: '101',
    name: 'tet'
};
/**
 * 
 * @param {Address} address 
 */
function customer(address) {
    
}
